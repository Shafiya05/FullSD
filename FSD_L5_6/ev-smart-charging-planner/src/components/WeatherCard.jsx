import {useEffect,useState} from 'react';import axios from 'axios';
const cities={Bangalore:[12.9716,77.5946],Mysore:[12.2958,76.6394],Chennai:[13.0827,80.2707],Hyderabad:[17.385,78.4867]};const details=c=>c===0?['☀️','Clear sky']:c<=3?['⛅','Partly cloudy']:c<=48?['🌫️','Foggy']:c<=67?['🌦️','Rainy']:c<=77?['❄️','Snowy']:['⛈️','Thunderstorms'];
function WeatherCard({city}){const [weather,setWeather]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState(false);useEffect(()=>{const controller=new AbortController(),[latitude,longitude]=cities[city];(async()=>{setLoading(true);setError(false);try{const r=await axios.get('https://api.open-meteo.com/v1/forecast',{params:{latitude,longitude,current:'temperature_2m,weather_code'},signal:controller.signal});setWeather({temperature:r.data.current.temperature_2m,code:r.data.current.weather_code,time:new Date()})}catch(err){if(err.name!=='CanceledError')setError(true)}finally{if(!controller.signal.aborted)setLoading(false)}})();return()=>controller.abort()},[city]);const d=weather&&details(weather.code);return <section className="card fade-in"><div className="heading-row"><div><h2 className="heading">🌤️ Live Weather</h2><p className="subtitle">Live conditions for your charging city.</p></div>{d&&<span className="weather-icon">{d[0]}</span>}</div>{loading&&<p className="subtitle">Loading weather...</p>}{error&&<p className="error">Unable to fetch weather.</p>}{weather&&!loading&&!error&&<dl className="stats weather-stats"><div className="stat"><dt>City</dt><dd>{city}</dd></div><div className="stat"><dt>Temperature</dt><dd>{weather.temperature}°C</dd></div><div className="stat"><dt>Condition</dt><dd>{d[1]}</dd></div><div className="stat"><dt>Refresh Time</dt><dd>{weather.time.toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit',second:'2-digit'})}</dd></div></dl>}</section>};export default WeatherCard
